{
  "$schema": "sign_in_draft",
  "$id": "2a",
  "title": "Sign In Schema",
  "description": "A schema for the sign in step in the flow.",
  "type": "object",
  "properties": {
    "username": {
      "type": "string",
      "minLength": 1
    },
    "password": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": ["username", "password"]
}
